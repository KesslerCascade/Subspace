#include <iup.h>
#include "subspace.h"
#include "lang/lang.h"

class SubspaceUI;
class SettingsPage {
    Subspace *ss;
    SubspaceUI *ui;

    Ihandle *h;
    Ihandle *parent;
    strref name;
    string imgname;
    string title;
    bool visible;

    bool make(Ihandle *list);
    bool update();
}