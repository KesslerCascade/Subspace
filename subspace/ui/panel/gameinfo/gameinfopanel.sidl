#include "ui/panel/panel.sidl"

class GameInfoPanel extends Panel {
    Ihandle *notrunning;
    Ihandle *playbtn;
    Ihandle *loading;
    Ihandle *progressbar;
    Ihandle *atmenu;
    Ihandle *tutorial;
    Ihandle *info;
    Ihandle *zbox;

    Ihandle *shipname;
    Ihandle *shiptype;
    Ihandle *sector;
    Ihandle *difficulty;

    Ihandle *statsbox;
    Ihandle *seed;

    string loadpct;
    Ihandle *cursub;

    factory create(SubspaceUI *ui);
    override make;
    override update;
}