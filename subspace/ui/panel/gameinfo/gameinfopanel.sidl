#include "ui/panel/panel.sidl"

class RunInfo;
class GameInfoPanel extends Panel {
    Ihandle *notrunning;
    Ihandle *playbtn;
    Ihandle *loading;
    Ihandle *progressbar;
    Ihandle *atmenu;
    Ihandle *tutorial;
    Ihandle *info;
    Ihandle *zbox;

    Ihandle *shipname;
    Ihandle *shiptype;
    Ihandle *sector;
    Ihandle *difficulty;
    Ihandle *seed;
    Ihandle *result;

    Ihandle *statsbox;
    int statcols;
    int statitems;
    int64 runupdate;

    string loadpct;
    Ihandle *cursub;

    factory create(SubspaceUI *ui);
    override make;
    override update;
    unbound void updateRun(RunInfo *run, bool force);
}