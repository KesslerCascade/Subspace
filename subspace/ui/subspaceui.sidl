#include <cx/taskqueue.sidl>
#include "subspace.h"
#include "lang/lang.h"

class MainWin;
class OptionsWin;
[methodprefix ssui] class SubspaceUI {
    Subspace *ss;
    object:TaskQueue uiq;
    bool started;

    object:MainWin main;
    object:OptionsWin options;

    factory create(Subspace *subspace);
    bool init();
    bool shutdown();

    void start();
    void stop();

    void update();
    void updateMain(strref panel);
    void updateOptions(strref page);
}
