#include <cx/taskqueue.sidl>
#include <iup.h>
#include <ui/image/image.sidl>

class IupImageAttachDispatch extends Task {
    Ihandle *h;
    string attr;
    object:Image img;

    factory create(Ihandle *hdl, strref attr, Image *img);
}