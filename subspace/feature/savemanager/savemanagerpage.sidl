#include "ui/page/settingspage.sidl"
#include "gamemgr/gameinst.sidl"

class SaveManager;
class SaveManagerPage extends SettingsPage {
    SaveManager* feature;

    Ihandle *savenametext;

    factory create(SaveManager* feature, SubspaceUI* ui);
    override make;
    override update;
}