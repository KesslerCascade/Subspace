#include "ui/page/settingspage.cxh"
#include "feature/screenshot/screenshot.cxh"

class Screenshot;
class ScreenshotPage extends SettingsPage {
    Screenshot* feature;

    Ihandle *fmtpng;
    Ihandle *fmtjpg;
    Ihandle *fmtradio;
    Ihandle *jpgqval;
    Ihandle *jpgqvalnum;
    Ihandle *namefmttext;
    Ihandle *nameextlabel;

    Ihandle *manlbl;
    object:SSLocControls mloc;
    object:SSLocControls aloc;

    Ihandle *sound;
    Ihandle *hidemouse;
    Ihandle *hidepause;
    Ihandle *hideinfoblock;
    Ihandle *metadata;

    Ihandle *autoach;
    Ihandle *autorfs1;
    Ihandle *autorfs2;
    Ihandle *autorfs3;
    Ihandle *autowinfight;
    Ihandle *autodestroyed;
    Ihandle *autogameover;
    Ihandle *autovictory;
    Ihandle *autocredits;

    factory create(Screenshot* feature, SubspaceUI* ui);
    override make;
    override update;
    override show;

    void updateFormatActive(strref fmt);
}

class SSLocControls
{
    ScreenshotPage *page;
    Subspace *ss;
    bool isauto;

    Ihandle *h;
    Ihandle *global;
    Ihandle *globalopen;
    Ihandle *rundir;
    Ihandle *custom;
    Ihandle *ctext;
    Ihandle *cbrowse;

    factory create(ScreenshotPage *page, bool isauto);
    bool make();
    void updateButtons(int32 loc);
    bool update();
}