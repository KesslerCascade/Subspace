#include "ui/page/settingspage.sidl"
#include "gamemgr/gameinst.sidl"

class RunTracker;
class RunTrackerPage extends SettingsPage {
    RunTracker* feature;

    Ihandle *rundirtext;

    factory create(RunTracker* feature, SubspaceUI* ui);
    override make;
    override update;
}